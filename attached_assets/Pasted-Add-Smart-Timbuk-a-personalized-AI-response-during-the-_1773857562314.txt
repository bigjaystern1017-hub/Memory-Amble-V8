Add Smart Timbuk — a personalized AI response during the co-creation phase. Here is what needs to be built:
1. In server/routes.ts, add a new POST endpoint /api/smart-confirm. It receives: userName (string), objectName (string), userAssociation (string), stopName (string). It calls OpenAI gpt-4o-mini with this system prompt: 'You are Timbuk, a warm memory coach. The user just described their vivid association for a memory palace object. Respond in 15 words or less. Be specific to what they said — reference their actual association, not just the object. If their association is creative or funny, celebrate it. If their input seems confused (like a question, or unrelated to placing an object), gently redirect: tell them the object and ask what they see happening there. Keep responses warm, short, punchy. Never say brilliant or perfect — find fresher language. Examples: user says James Bond Goldfingers phone for telephone booth → Very 007 — that telephone booth is never leaving your palace. User says my granddaughter for rocking horse → Your granddaughter on that rocking horse — she will pull it right back every time. User says what is this → No worries — your object here is a [object]. What do you picture happening there?'
2. In beat-engine.ts, find the co-creation confirmation messages — currently hardcoded templates like 'brilliant, that one is locked in' and 'perfect, keep walking'. Replace these with a flag that signals to amble.tsx that a smart confirmation is needed.
3. In amble.tsx, when the co-creation confirmation flag is detected, call /api/smart-confirm with the current object, the user's typed association, their name, and the current stop name. Display Timbuk's response as the confirmation message. Show a brief loading state while the call completes — maybe Timbuk's avatar pulsing or a subtle 'Timbuk is thinking...' indicator for 1-2 seconds.
4. Keep the existing hardcoded confirmations as a fallback — if the API call fails for any reason, fall back to the template response so the session never breaks.
5. Apply the same smart confirmation to the recall phase — when a user gets an answer right, instead of the generic 'Yes! accordion! That is the palace at work' — call smart-confirm with the object and their answer and generate a brief celebratory response.
Do not change anything else. Guest users and logged-in users should both get smart confirmations