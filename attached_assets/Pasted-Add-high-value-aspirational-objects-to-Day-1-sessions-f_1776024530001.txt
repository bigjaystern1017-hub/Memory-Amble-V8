Add high-value aspirational objects to Day 1 sessions for price anchoring. Here's exactly what to do:

1. In server/routes.ts, add this new array right after the existing RANDOM_OBJECTS array:

const DAY_1_PREMIUM_OBJECTS = [
  "$100 bill",
  "diamond ring",
  "gold watch",
  "pearl necklace",
  "vintage champagne bottle",
];

2. In server/routes.ts, update the assignItemsSchema to accept an optional dayCount field. Find:

const assignItemsSchema = z.object({
  stops: z.array(z.string()).min(1).max(10),
  category: z.enum(["objects", "names", "practical"]).default("objects"),
});

Replace with:

const assignItemsSchema = z.object({
  stops: z.array(z.string()).min(1).max(10),
  category: z.enum(["objects", "names", "practical"]).default("objects"),
  dayCount: z.number().optional(),
});

3. In server/routes.ts, in the /api/assign-objects endpoint, after the line that creates the assignments array:

const assignments = stops.map((stop, i) => ({
  stopName: stop,
  object: items[i],
}));

Add this block right before res.json({ assignments }):

// On Day 1, ensure at least one premium object for price anchoring
if (parsed.data.dayCount === 0 && parsed.data.category === "objects" && assignments.length > 0) {
  const premiumItem = DAY_1_PREMIUM_OBJECTS[Math.floor(Math.random() * DAY_1_PREMIUM_OBJECTS.length)];
  const replaceIndex = Math.floor(Math.random() * assignments.length);
  assignments[replaceIndex] = { ...assignments[replaceIndex], object: premiumItem };
}

4. In client/src/components/beat-engine.ts, add these entries to the ITEM_EMOJIS object:

"$100 bill": "💵",
"diamond ring": "💎",
"gold watch": "⌚",
"pearl necklace": "📿",
"vintage champagne bottle": "🍾",

5. In client/src/pages/amble.tsx, find the fetchAssignments function. It currently sends:

body: JSON.stringify({
  stops: currentState.stops,
  category: currentState.category,
}),

Replace with:

body: JSON.stringify({
  stops: currentState.stops,
  category: currentState.category,
  dayCount: currentState.dayCount,
}),

6. Also in amble.tsx, find the expansion-stop-2 case in processUserInput where it calls /api/assign-objects. It currently sends:

body: JSON.stringify({ stops: [newStop1, newStop2], category: s.category }),

Replace with:

body: JSON.stringify({ stops: [newStop1, newStop2], category: s.category, dayCount: s.dayCount }),

Do not change anything else. Do not modify any other endpoints, routing logic, or Timbuk copy.